 export enum PaymentMethod {
     CASH = 'cash',
     CARD = 'card',
     POSTPAID = 'postpaid',
     TRANSFER = 'transfer',
}

export enum PaymentStatus {
     UNPAID = 'unpaid',
     PAID = 'paid',
     CANCELLED = 'cancelled',
}
